/*
 * Bhopcord shared assets (bundled at build time).
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

import logoBase64 from "file://../assets/logo.png?base64";

export const BHOPCORD_LOGO_SRC = `data:image/png;base64,${logoBase64}`;
